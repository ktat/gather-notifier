# improve notification distinction

When wave event happens, a DOM is added and in it,
`ktat-test waved to you` is contained.
`ktat-test` is user name, so ` waved to you` is the message for detection.
